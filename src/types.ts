export interface Project {
    imageUrl: string,
    title: string,
    shortDescription: string,
    context: string,
    description: string,
    difficulties: string,
    experience: string,
    tags: string[],
    caroussel: string[]
}