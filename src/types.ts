export interface Project {
    title: string,
    shortDescription: string,
    context: string,
    description: string,
    difficulties: string,
    experience: string,
    tags: string[],
    caroussel: string[]
}